#include<bits/stdc++.h>
using namespace std;

int main()
{
	int n,t;
	cout<<"enter the number of test cases\n";
	cin>>t;
	while(t--)
	  {
		cout<<"Enter the number of elements\n";
		cin>>n;
		int a[n];
		for(int i=0; i<n; i++)
		{
			cin>>a[i];
		}
		sort(a,a+n);
		for(int i=0; i<n-2; i++)
		{
			cout<<a[i]<<" ";
		}
		cout<<endl;
	}
	return 0;
}
